export enum BASIC_ENUM {
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
}

export enum USER_ENUM {
    ONLINE = "ONLINE",
    OFFLINE = "OFFLINE",
    INACTIVE = "INACTIVE",
}

export enum TABLE_ENUM {
    IN_USE = "IN_USE",
    NOT_USE = "NOT_USE",
    INACTIVE = "INACTIVE",
}
