export enum ROUTES {
    //Routes Page
    DEFAULT = "/",
    PAYMENT_METHOD = "/payment-method",
    ROLE = "/role",
    SETTING = "/setting",
    ACCOUNT = "/account",
    WORKSESSION = "/worksession",
    SHIFT = "/shift",
    VOID_REASON = "/void-reason",
    LOCATION = "/location",
    TABLE = "/table",
}
